// QuestionDetail.js
import React from 'react';

const QuestionDetail = ({ match }) => {
  // Use match.params.id to fetch and display the details of a specific question
  return (
    <div>
      <h2>Question Detail</h2>
      {/* Display question details */}
    </div>
  );
};

export default QuestionDetail;